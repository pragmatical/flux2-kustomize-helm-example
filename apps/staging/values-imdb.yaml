apiVersion: apps/v1
kind: Deployment
metadata:
  name: imdb
  namespace: imdb
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: imdb
    spec:
      containers:
        - name: app
          args:
          - --in-memory
          - --zone
          - staging
          - --region
          - staging